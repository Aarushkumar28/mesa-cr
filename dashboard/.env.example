# Code Reviewer Dashboard — Environment Variables
# Copy this file to .env and fill in your values.

# Backend API URL (FastAPI server)
VITE_API_BASE_URL=http://localhost:8000

# GitHub App name (for install link) — must match the real GitHub App slug
VITE_GITHUB_APP_NAME=codereview-mesa
